spring:
  datasource:
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    url: jdbc:log4jdbc:mysql://localhost:3306/chyou?serverTimezone=UTC&allowMultiQueries=true
    #url: jdbc:log4jdbc:mysql://chyou.cafe24.com:3306/chyou?serverTimezone=UTC&allowMultiQueries=true
    username: chyou
    password: chyouchyou!!
  thymeleaf:
    cache: false

mybatis:
  mapper-locations: classpath:mappers/*-mapper.xml
  configuration:
    map-underscore-to-camel-case: true

spring.thymeleaf.cache: false
logging:
  level:
    org:
      springframework:
        security: INFO
        web: DEBUG

debug: true